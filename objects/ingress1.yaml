apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-myapp
spec:
  rules:
  - host: myapp.com
    http:
        paths:
        - backend:
             serviceName: myapp-internal-services
             servicePort: 8080
